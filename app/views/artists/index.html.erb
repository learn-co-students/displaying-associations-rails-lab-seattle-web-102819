<h1>Artists</h1>

<ul>
  <% @artists.each do |a| %>
    <li>
      <%= link_to a.name, artist_path(a) %>
      <!-- pluralize helper method! -->
      <%= a.song_count %> <%= "songs".pluralize(a.song_count) %>
    </li>
  <% end %>
</ul>